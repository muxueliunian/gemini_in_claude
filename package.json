{
  "name": "gemini-plugin-cc",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code marketplace plugin that delegates tasks to the local Antigravity CLI (agy, headless).",
  "type": "module",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "license": "Apache-2.0"
}
